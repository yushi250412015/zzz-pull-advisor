# ZZZ 抽卡参谋（Pull Advisor）企划方案

> 版本：v3（2026-08 架构重构版：核心要点定稿 + SOLID 逐条落实 + UI/核心/datasource 三层拆分，审计基准 160 项测试全绿；详见 docs/ARCHITECTURE.md）
> 版本：v2（2026-08 再审视版，在 v1 基础上按现状重写计划）
> 定位：基于「概率 × 真实边际效用 × 风险」的《绝区零》抽卡辅助决策工具。

## 0. v2 再审视结论（2026-08，审计基准：129 项测试全绿、构建通过）

### 现状
- **31 位角色**（18 S + 13 A；此前文档误记 39 已勘误）；meta 校准 30/31（维琳娜 null 不臆造）；enName 全覆盖；官方公告/BWIKI/权威博主多源审计零冲突
- 五层算法 + 观测闭环（Prydwen/权威博主/卡特亚/毕加丶/梦轩dada + **Kalman 后验** + **CVaR 下行风险** + meta 接入引擎）
- 数据工具链：官方公告 API 封装、版本事实提取、抽卡记录导入 CLI、公告归档、**B站视频自动发现（图爬取）与批量消化（登录态）**
- UI 面板：资源 / box / 四池状态 / 账号同步（UID → 本地服务 → 四池刷新）/ 角色推荐 / 音擎邦布推荐 / 官方情报 / 强度可信度 / 权重面板 / 多场景 MCTS（纯强度推荐；机制速查已移除）

### 本轮修复（P0，一致性）
1. **后验接入引擎**：可信度面板的 Kalman 后验此前只展示不参与计算——已统一口径（metaUtility / recommendCharacter / MCTS 共用 metaMap）
2. **配置单源**：VERSION_RESOURCES 由 decision.js 导出，main.js 引用（消除双份漂移）
3. **文档漂移修正**：README/HANDOVER 角色数、测试数同步

### 本轮增强（P1）
4. 欧非统计进 UI（四池面板：平均抽/S、独家胜率、全账号抽数）
5. 官方公告提及角色接线（官方情报面板展示正文提及清单）

### 框架层（已实现且有测试、未接数据流——待可信数据源后启用，保留不删）
- 六维评价向量（vector.js）：需逐角色六维权威数据（暂无）
- TrueSkill 1v1（rating.js）：需队伍对抗观测（评论区仅零星）
- Kalman 多观测（weightedObservation/kalmanFilterSeries）：kalmanStep 已在 meta 后验中使用
- parse.js（parseGachaHistory/computeLuckStats）：P5 早期 API，与 import-summary.js 功能重叠，保留测试待统一

### 下一步计划（v2.1）（2026-08 推进一轮，状态见 CHANGELOG 与 docs/data-sources.md）
1. ~~希格莉德倍率补全~~ ✅ GachaBase 测试服 datamined 已补全（data-sources.md §13）；HHW 521 / Icy Veins 403 待恢复后交叉复核；实装 08-19 后复核数值
2. ~~观测源持续消化~~ ✅ 年度T0榜已消化（毕加 BV1GUZ1B8E8L，4 条观测，§12.9）；⏳ 毕加/卡特亚视频库其余条目待逐条取结论
3. 音擎/邦布价值参数敏感性校准（有实测数据后；scripts/sensitivity-equipment.mjs 已就绪）
4. UI 层 jsdom 测试 ✅（前轮 +8）/ 移动端适配 ✅（560px 断点增强；可继续按需优化）
5. ~~3.2 版本数据更新~~ ⏳ SOP 已演练确认 3.2 未进窗口（3.1 至 09/09）；等 3.2 公告进窗口后执行

## 0.1 一句话定位（v1 原文）

帮玩家回答「这个版本该不该抽、抽多少、抽谁」，输出 **帕累托策略集 + 可解释理由 + 概率/风险**，**不强制给出唯一答案**。

---

## 1. 核心原则（含「后续可修改」）

1. **数据驱动**：角色 / 体系 / 队伍 / 权重全部是数据（JSON），新版本 = 更新数据，不改代码。
2. **模块可替换**：每个模块走统一接口，算法可整体替换（例如成型度模型可从「规则」升级到「矩阵分解 / 图神经网络」）。
3. **纯函数核心 + 适配器边界**：核心算法是可单测的纯函数；数据源 / IO 走适配器，可插拔。
4. **效用是分布，不是单点**：输出区间 + 置信度，而非一个确定分数。
5. **非官方定位**：界面标注「非官方、仅供参考」。

---

## 2. 总体架构

```
src/
├── core/                 # 纯算法（可单测、可替换）
│   ├── gacha/            # 概率层
│   ├── utility/          # 效用层
│   ├── bayes/            # 时序贝叶斯层
│   └── decision/         # 决策优化层
├── models/               # 数据实体（可替换、随版本更新）
│   ├── systems.js        # 体系实体
│   ├── characters.js     # 角色
│   └── teams.js          # 队伍 + 观测样本
├── datasource/           # 数据获取适配器（可插拔）
│   ├── gacha-log.js      # 本地日志 URL 导入
│   ├── hoyolab.js        # HoYoLAB 战绩（后续接入）
│   └── manual.js         # 手动录入
├── state/                # 应用状态
├── ui/                   # 界面
└── config.js             # 用户偏好与权重
```

依赖方向单向：`ui → state → core/models`，`core` 不依赖任何上层。

---

## 3. 模块契约（保证可替换的关键）

每个模块 = 一个稳定接口 + 一个默认实现；换算法只换实现、不动接口。

| 模块 | 接口（输入 → 输出） | 默认实现 | 可替换为 |
|---|---|---|---|
| 概率 | `simulate(state, budget) → { P角色, P专武, P空手, 期望抽数 }` | 马尔可夫概率表 + 蒙特卡洛 | 精确动态规划 |
| 效用 | `teamUtility(team, box, ctx) → { vector6, confidence }` | 多维向量 + 置信度 | 矩阵分解 / GNN |
| 成型度 | `formation(box, system) → F∈[0,1]` | 可学习贡献向量 θ | 规则 / 学习模型 |
| 贝叶斯 | `update(prior, obs, t) → posterior` | 高斯共轭先验 | MCMC 采样 |
| 决策 | `decide(box, res, banners, model) → Pareto 策略集` | 蒙特卡洛 + 帕累托筛选 | 有限视野 DP |
| 技能 | `estimateSkill(box, abyss?) → k_op` | 手动低/中/高 | HoYoLAB 战绩反推 |

---

## 4. 数据获取方案（已确认）

| 数据 | 方式 | 状态 |
|---|---|---|
| 抽卡记录 → box + 保底 | 本地日志 URL（不碰账号密码） | ✅ 采用 |
| 深渊表现 → k_op | 手动低/中/高 + 可选填表现 | ✅ 采用，预留 HoYoLAB 自动反推 |
| 队伍→表现 社区样本 | 手动维护观测表 + 预留投稿入口 | ✅ 采用，不爬虫 |

---

## 5. 「后续可修改」如何落地（逐条机制）

1. **接口稳定**：模块间只通过第 3 节表格的接口通信，实现内部可任意重写。
2. **数据与代码分离**：角色/体系/队伍/权重全在 `models/` 与 `config.js`，新增体系 = 加一条 JSON，不动代码。
3. **适配器模式**：数据源（日志 URL / HoYoLAB / 手动）都是适配器，后续加新源 = 新增一个文件、实现同一接口。
4. **k_op 可升级**：`estimateSkill` 接口先挂手动实现，HoYoLAB 战绩明细确认后换自动实现，调用方不变。
5. **模型可替换**：成型度 / 效用 / 决策的实现可插拔，先用轻量模型（贝叶斯 / 矩阵分解），数据积累后再上重模型。
6. **数据 schema 版本化**：数据文件带 `version` 字段，版本升级走迁移，不破坏旧数据。

---

## 6. 分阶段实施

| 阶段 | 内容 | 验收 |
|---|---|---|
| P1 | 概率层：马尔可夫概率表 + 蒙特卡洛 + 单测 | 概率表与游戏机制对齐 |
| P2 | 效用层：成型度 / 协同 / 多维向量 / 边际效用 + 数据模型（占位数据） | 单测 + 闭环跑通 |
| P3 | 决策层：总效用 + 帕累托 + 风险/机会成本 | 输出策略集 |
| P4 | 贝叶斯时序框架 + 观测数据表 | 后验更新跑通 |
| P5 | 数据源适配器（日志 URL / 手动）+ UI + 真实数据 + 发布 | 线上可用 |

---

## 7. 风险与约束

1. **数据维护成本**：每 6 周更新角色/体系/观测数据，是项目存活关键。
2. **观测数据量不足**：现有样本量养不起深度模型，从贝叶斯/矩阵分解等轻量模型起步。
3. **HoYoLAB 战绩明细不确定**：可能只给汇总、不给队伍级明细，k_op 自动反推需待确认。
4. **版权**：仅用角色名与数值，不用立绘/音频素材。
5. **准确性有限**：一切输出为参考，界面标注非官方。

---

## 8. 待办（下一步）

- [x] 用户确认本方案
- [x] 按 P1 开始实现概率层（已完成，见 `src/core/gacha/`）

## 9. 技术选型（v2，调研后升级）

调研发现的关键可复用项目与模型：

- **概率层**：参考成熟抽卡概率包（GGanalysis 等）的反推软保底曲线，改为「逐抽概率表」，替代线性近似
- **效用层（核心）**：用 **贝叶斯技能评分（Weng-Lin / TrueSkill / Bradley-Terry）** 学出「角色/队伍隐式强度」+ 不确定性，替代硬编码灵魂角色与线性加权
- **理论先验**：参考现有 ZZZ 战斗模拟器（如 ZSim）的数值模拟结果作为先验
- **时序**：用 **Kalman 滤波（状态空间模型）** 替代朴素贝叶斯，建模「隐效用随时间漂移 + 噪声观测」
- **决策**：MDP + **CVaR 下行风险**度量，比单纯概率更严谨

核心原则：优先「可学习 + 带不确定性 + 少样本友好」的成熟模型（贝叶斯评级/滤波），不引入数据量养不起的深度模型。
